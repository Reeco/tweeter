%h3{:style => "text-align:center;"}
   Tweeter Search Result
%hr

- if @tweets
   - @tweets.each do |i|
      %p= i.created_at.to_s + " ---> " + i.text
  else
   - %p
      ERROR! Please select the type and fill keyword box properly
